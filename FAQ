•如何进行软件的下载与运行？

可以在github上直接复制源代码，然后再自己的编译器上编译，也可以直接下载已经编译好的版本，直接在c++环境下运行。以后也会加入更多的功能。

•软件有什么样的功能？

可以计算部分平面图形的周长和面积以及部分立体图形的体积和表面积，以及可以判断输入的数据（边长，半径等）能否构成相应图形。

•如何使用软件？

编译后，先选择所要求的图形是平面还是立体，然后再选择图形，然后输入必要的数据，即可得到想要的数据。
